from os.path import dirname, join

enmap_berlin = join(dirname(__file__), 'enmap_berlin.bsq')
enmap_srf_library = join(dirname(__file__), 'enmap_srf_library.gpkg')
hires_berlin = join(dirname(__file__), 'hires_berlin.bsq')
landcover_berlin_point = join(dirname(__file__), 'landcover_berlin_point.gpkg')
landcover_berlin_polygon = join(dirname(__file__), 'landcover_berlin_polygon.gpkg')
library_berlin = join(dirname(__file__), 'library_berlin.gpkg')
veg_cover_fraction_berlin_point = join(dirname(__file__), 'veg-cover-fraction_berlin_point.gpkg')
